CREATE TABLE tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task TEXT NOT NULL
);

-- Este código cria uma tabela chamada tasks com duas colunas: id, que é a chave primária da tabela, e task, que contém o texto da tarefa.